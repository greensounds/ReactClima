/*var names = ['Glow', 'Gab', 'Chuy'];

names.forEach(function (name) {
	console.log('forEach', name);
});

//Arrow function
names.forEach((name) => {
	console.log('arrowFunc', name);
})*/

//var returnMe = (name) => name + '!';

//console.log(returnMe('jes'));

function add(a, b) {
	return a + b;
}

console.log(add(1,5))

var suma = (a,b) => a + b;

var sumados = (d,e) => {
	return d + e;
	
}
console.log(sumados(10,10));
console.log(suma(5,7)); 